# Dependencies for the ASR container.

# ── Server ────────────────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.2

# ── ASR core ──────────────────────────────────────────────────────────────────
# faster-whisper uses CTranslate2 — significantly faster + lower VRAM than
# the original OpenAI Whisper at the same accuracy level.
faster-whisper==1.1.1

# ── Audio I/O ─────────────────────────────────────────────────────────────────
# soundfile reads WAV/FLAC/OGG via libsndfile (no ffmpeg dependency for WAV).
soundfile==0.12.1

# ── Numerics ──────────────────────────────────────────────────────────────────
numpy>=1.24.0,<2.0.0

# ── HTTP (required by faster-whisper internally for model downloads) ──────────
requests==2.32.3

# ── Cross-validation / evaluation (run outside the container) ─────────────────
# Install these locally: pip install jiwer tqdm requests
# jiwer==3.0.4